[user]
	name = nicholausk
	email = nicholausk@users.noreply.github.com
[core]
        editor = vim
        excludesfile = ~/.gitignore
[alias]
	loggy = log --oneline --graph --all --decorate
[filter "lfs"]
	clean = git-lfs clean %f
	smudge = git-lfs smudge %f
	required = true
